import { ProdStyleDirective } from './prod-style.directive';

describe('ProdStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdStyleDirective();
    expect(directive).toBeTruthy();
  });
});
